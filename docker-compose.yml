version: '3.8'
services:
  metrolist:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - ./cache:/app/cache
      - ./music.db:/app/music.db
    environment:
      - APP_PORT=3000
      - APP_HOST=0.0.0.0
    restart: unless-stopped